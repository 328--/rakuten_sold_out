<div class="starter-template" style="margin-top: 5em;">

  <h4>Select a Genre <%= form_tag({action: :search}, remote: true) do %><%= select_tag 'page[name]', options_from_collection_for_select(@genres, :id, :name), :include_blank => true %><%= submit_tag "Send" %><% end %></h4>

  <br />
  
  <div id='ajax_area'>
	<%= render :partial => 'show', :object => @search_result %>
  </div>

</div>

